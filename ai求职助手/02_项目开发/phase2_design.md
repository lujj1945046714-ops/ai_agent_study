# Phase 2 设计文档：多轮对话与主动建议

**创建时间**: 2026-02-28
**目标**: 让 AI Agent 从"工具"变成"助手"

---

## 🎯 设计目标

### 核心价值
将单次问答的工具升级为能够：
1. **记住上下文** - 记住分析过的职位、推荐过的项目
2. **支持追问** - "再推荐几个项目"、"这个职位怎么样"
3. **主动建议** - 分析完职位后主动问："要不要看看学习路径？"
4. **智能规划** - 自动制定3个月/6个月学习计划

### 用户体验提升
**之前**:
```
用户: 帮我分析这个职位
Agent: [分析结果]
用户: 推荐学习项目
Agent: [推荐结果]
```

**现在**:
```
用户: 帮我分析这个职位
Agent: [分析结果]
       匹配度71分，主要缺口是LangChain和Docker。
       要不要我帮你：
       1. 推荐学习项目
       2. 制定学习计划
       3. 继续看其他职位

用户: 推荐学习项目
Agent: [推荐3个项目]
       这些项目可以帮你提升LangChain到level 3。
       需要我再推荐几个吗？或者制定详细的学习计划？

用户: 再推荐几个
Agent: [记住之前推荐过的，推荐新的项目]
```

---

## 📋 功能清单

### 1. 对话历史管理 ⭐
**目标**: 记住整个会话的上下文

**需要记住的信息**:
- 已分析的职位列表
- 每个职位的分析结果
- 每个职位的匹配结果
- 已推荐的学习项目
- 用户的选择和偏好

**数据结构**:
```python
class ConversationMemory:
    def __init__(self):
        self.analyzed_jobs = {}      # {job_id: analysis_result}
        self.match_results = {}      # {job_id: match_result}
        self.recommended_projects = {} # {job_id: [projects]}
        self.user_preferences = {}   # 用户偏好
        self.conversation_history = [] # 对话历史

    def add_job_analysis(self, job_id, analysis):
        """记录职位分析"""

    def add_match_result(self, job_id, match):
        """记录匹配结果"""

    def add_recommended_projects(self, job_id, projects):
        """记录推荐项目"""

    def get_context_summary(self):
        """获取上下文摘要"""

    def has_analyzed_job(self, job_id):
        """检查是否已分析过该职位"""
```

### 2. 主动建议引擎 ⭐
**目标**: 在合适的时机主动提出建议

**建议时机**:
1. **分析完职位后**:
   - 匹配度 < 60: "这个职位匹配度较低，要不要看看其他的？"
   - 匹配度 60-80: "匹配度不错！要不要我推荐学习项目来弥补技能缺口？"
   - 匹配度 > 80: "匹配度很高！要不要我帮你准备面试？"

2. **推荐完项目后**:
   - "这些项目可以帮你提升 [技能]。需要我制定详细的学习计划吗？"
   - "需要我再推荐几个项目吗？"

3. **发现多个职位时**:
   - "我看到有3个相关职位，要不要我帮你对比一下？"

**实现**:
```python
class ProactiveSuggestionEngine:
    def suggest_after_analysis(self, match_score, skill_gaps):
        """分析后的建议"""
        if match_score < 60:
            return {
                "message": "这个职位匹配度较低（{match_score}分），主要缺口：{gaps}。",
                "options": [
                    "推荐学习项目来提升技能",
                    "看看其他更匹配的职位",
                    "制定长期学习计划"
                ]
            }
        elif match_score < 80:
            return {
                "message": "匹配度不错（{match_score}分）！主要缺口：{gaps}。",
                "options": [
                    "推荐学习项目",
                    "制定学习计划",
                    "继续分析其他职位"
                ]
            }
        else:
            return {
                "message": "匹配度很高（{match_score}分）！",
                "options": [
                    "准备面试",
                    "优化简历",
                    "了解公司背景"
                ]
            }

    def suggest_after_recommendation(self, recommended_count):
        """推荐后的建议"""
        return {
            "message": f"已推荐 {recommended_count} 个项目。",
            "options": [
                "再推荐几个项目",
                "制定详细学习计划",
                "继续分析其他职位"
            ]
        }
```

### 3. 智能学习规划 ⭐
**目标**: 根据技能缺口自动制定学习计划

**规划维度**:
- **时间**: 3个月 / 6个月 / 12个月
- **优先级**: 必备技能 > 技术栈 > 加分项
- **难度**: 从易到难
- **依赖**: 先学基础，再学高级

**示例输出**:
```
📅 3个月学习计划

第1个月：基础技能
  Week 1-2: LangChain 基础
    - 项目：langchain-tutorials (⭐2.3k)
    - 目标：从 level 2 提升到 level 3
    - 里程碑：完成3个demo项目

  Week 3-4: Docker 入门
    - 项目：docker-curriculum (⭐5.2k)
    - 目标：从 level 0 提升到 level 2
    - 里程碑：能独立部署应用

第2个月：进阶实践
  Week 5-6: LangChain 进阶
    - 项目：langchain-ChatGLM (⭐8.5k)
    - 目标：深入理解 RAG 和 Agent
    - 里程碑：完成一个完整的 Agent 项目

  Week 7-8: 综合项目
    - 项目：自己的 AI Agent 项目
    - 目标：整合所学技能
    - 里程碑：上线一个可用的项目

第3个月：优化提升
  Week 9-10: 性能优化
    - 学习：Prompt 优化、缓存策略
    - 目标：提升系统性能

  Week 11-12: 准备面试
    - 整理项目经验
    - 准备技术问题
    - 模拟面试
```

**实现**:
```python
class LearningPlanner:
    def create_plan(self, skill_gaps, timeframe="3months"):
        """创建学习计划"""
        # 1. 按优先级排序技能缺口
        sorted_gaps = self._sort_by_priority(skill_gaps)

        # 2. 为每个技能分配时间
        time_allocation = self._allocate_time(sorted_gaps, timeframe)

        # 3. 推荐学习资源
        resources = self._recommend_resources(sorted_gaps)

        # 4. 设置里程碑
        milestones = self._set_milestones(sorted_gaps)

        return {
            "timeframe": timeframe,
            "phases": self._create_phases(time_allocation, resources, milestones)
        }
```

### 4. 上下文追问支持
**目标**: 理解用户的简短追问

**示例**:
```
用户: 帮我分析这个职位
Agent: [分析 job-001]

用户: 再推荐几个
Agent: [理解为：为 job-001 再推荐几个项目]

用户: 这个职位怎么样
Agent: [理解为：询问 job-001 的匹配情况]

用户: 对比一下
Agent: [理解为：对比已分析的多个职位]
```

**实现**:
```python
class ContextualUnderstanding:
    def __init__(self, memory: ConversationMemory):
        self.memory = memory

    def resolve_reference(self, user_input):
        """解析用户输入中的指代"""
        # "再推荐几个" → 为最近分析的职位推荐项目
        # "这个职位" → 最近分析的职位
        # "对比一下" → 对比所有已分析的职位

        if "再推荐" in user_input or "更多项目" in user_input:
            last_job = self.memory.get_last_analyzed_job()
            return {
                "intent": "recommend_more",
                "job_id": last_job
            }

        if "这个职位" in user_input or "它" in user_input:
            last_job = self.memory.get_last_analyzed_job()
            return {
                "intent": "query_job",
                "job_id": last_job
            }

        if "对比" in user_input:
            all_jobs = self.memory.get_all_analyzed_jobs()
            return {
                "intent": "compare_jobs",
                "job_ids": all_jobs
            }
```

---

## 🏗️ 架构设计

### 整体架构
```
┌─────────────────────────────────────────┐
│           User Input                     │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│    Contextual Understanding              │
│    - 解析指代                             │
│    - 理解意图                             │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│       ReAct Agent (Enhanced)             │
│    - 工具调用                             │
│    - 推理决策                             │
│    - 访问记忆                             │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│    Proactive Suggestion Engine           │
│    - 分析后建议                           │
│    - 推荐后建议                           │
│    - 主动提问                             │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│      Conversation Memory                 │
│    - 职位分析历史                         │
│    - 推荐项目历史                         │
│    - 用户偏好                             │
└─────────────────────────────────────────┘
```

### 数据流
```
1. 用户输入 → 上下文理解 → 解析意图
2. 意图 + 记忆 → ReAct Agent → 工具调用
3. 工具结果 → 更新记忆
4. 工具结果 → 主动建议引擎 → 生成建议
5. 结果 + 建议 → 返回用户
```

---

## 📁 文件结构

```
agent/
├── react_agent.py              # ReAct Agent（需要增强）
├── tools.py                    # 工具定义
├── memory.py                   # 对话记忆（新增）⭐
├── suggestion_engine.py        # 主动建议引擎（新增）⭐
├── learning_planner.py         # 学习规划器（新增）⭐
└── context_understanding.py    # 上下文理解（新增）⭐

test/
├── test_memory.py              # 记忆测试
├── test_suggestion_engine.py   # 建议引擎测试
├── test_learning_planner.py    # 规划器测试
└── test_phase2_integration.py  # Phase 2 集成测试
```

---

## 🔧 实现计划

### Step 1: 对话记忆系统（核心）
**文件**: `agent/memory.py`

**功能**:
- 记录职位分析历史
- 记录匹配结果
- 记录推荐项目
- 提供上下文查询接口

**预计时间**: 4小时

### Step 2: 主动建议引擎
**文件**: `agent/suggestion_engine.py`

**功能**:
- 分析后建议
- 推荐后建议
- 根据匹配度生成不同建议

**预计时间**: 3小时

### Step 3: 学习规划器
**文件**: `agent/learning_planner.py`

**功能**:
- 创建3/6/12个月学习计划
- 按优先级排序技能
- 设置学习里程碑

**预计时间**: 4小时

### Step 4: 上下文理解
**文件**: `agent/context_understanding.py`

**功能**:
- 解析指代（"这个"、"它"、"再来几个"）
- 理解简短追问
- 结合记忆推断意图

**预计时间**: 3小时

### Step 5: 增强 ReAct Agent
**文件**: `agent/react_agent.py`（修改）

**功能**:
- 集成记忆系统
- 集成建议引擎
- 支持上下文理解

**预计时间**: 3小时

### Step 6: 测试与集成
**文件**: `test/test_phase2_*.py`

**功能**:
- 单元测试
- 集成测试
- 多轮对话测试

**预计时间**: 3小时

---

## 📊 测试场景

### 场景 1: 单职位深度分析
```
用户: 帮我分析这个 AI Agent 工程师职位
Agent: [分析结果] 匹配度71分。要不要我推荐学习项目？

用户: 好的
Agent: [推荐3个项目] 需要我再推荐几个吗？

用户: 再推荐几个
Agent: [推荐另外3个项目，避免重复]

用户: 制定学习计划
Agent: [生成3个月学习计划]
```

### 场景 2: 多职位对比
```
用户: 帮我搜索 AI Agent 工程师职位
Agent: [找到5个职位] 要不要我帮你分析一下？

用户: 分析前3个
Agent: [分析3个职位] 要不要我帮你对比一下？

用户: 对比一下
Agent: [对比表格] 推荐职位2，匹配度最高。要详细分析吗？

用户: 详细分析职位2
Agent: [详细分析] 要不要推荐学习项目？
```

### 场景 3: 追问与上下文
```
用户: 分析这个职位
Agent: [分析 job-001]

用户: 匹配度怎么样
Agent: [理解为询问 job-001] 匹配度71分...

用户: 缺口是什么
Agent: [理解为询问 job-001 的技能缺口] 主要缺口是...

用户: 推荐项目
Agent: [为 job-001 推荐项目]
```

---

## 🎯 成功标准

### 功能完整性
- [x] 对话记忆系统
- [x] 主动建议引擎
- [x] 学习规划器
- [x] 上下文理解
- [x] ReAct Agent 集成

### 用户体验
- [x] 支持多轮对话
- [x] 理解简短追问
- [x] 主动提出建议
- [x] 生成学习计划

### 测试覆盖
- [x] 单元测试（每个模块）
- [x] 集成测试（完整流程）
- [x] 多轮对话测试

---

## 💡 技术要点

### 1. 记忆持久化
```python
# 使用 JSON 文件持久化
class ConversationMemory:
    def save(self, filepath):
        """保存到文件"""
        with open(filepath, 'w') as f:
            json.dump(self.to_dict(), f, indent=2)

    def load(self, filepath):
        """从文件加载"""
        with open(filepath, 'r') as f:
            data = json.load(f)
            self.from_dict(data)
```

### 2. 上下文窗口管理
```python
# 只保留最近 N 轮对话
class ConversationMemory:
    def __init__(self, max_history=10):
        self.max_history = max_history
        self.conversation_history = []

    def add_turn(self, user_input, agent_output):
        self.conversation_history.append({
            "user": user_input,
            "agent": agent_output,
            "timestamp": datetime.now()
        })

        # 保持窗口大小
        if len(self.conversation_history) > self.max_history:
            self.conversation_history.pop(0)
```

### 3. 意图识别
```python
# 使用关键词 + 规则
class ContextualUnderstanding:
    INTENT_PATTERNS = {
        "recommend_more": ["再推荐", "更多项目", "还有吗"],
        "query_job": ["这个职位", "它", "怎么样"],
        "compare_jobs": ["对比", "比较"],
        "create_plan": ["学习计划", "规划", "路线图"]
    }

    def detect_intent(self, user_input):
        for intent, patterns in self.INTENT_PATTERNS.items():
            if any(p in user_input for p in patterns):
                return intent
        return "unknown"
```

---

## 🚀 开始实现

让我们从 Step 1 开始：实现对话记忆系统。

准备好了吗？
